${{ className }}-font-name: '{{ fontName }}';
${{ className }}-font-path: '{{ fontPath }}';
{% for glyph in glyphs%}${{ className }}-{{ glyph.name }}: "\{{ glyph.unicode[0].charCodeAt(0).toString(16) }}";
{% endfor %}
